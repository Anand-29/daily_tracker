import 'package:flutter/material.dart';

class RequirementCard extends StatelessWidget {
  const RequirementCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Column(
        children: [
          Text("data"),
        ],
      ),
    );
  }
}
